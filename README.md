# automated-AWS-IAM-security-response-system
This Project will automatically detect and respond to exposed IAM credentials using AWS Health events, disable compromised access keys immediately  and notify the security team
